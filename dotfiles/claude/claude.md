### Claude Code Config ###

`settings.json` -> `~/.claude/settings.json`
`statusline-command.sh` -> `~/.claude/statusline-command.sh`

Status line shows: model | cwd | git branch (+dirty) | context % | cost | duration, spread across the terminal width.
